// Deprecated legacy admin script (sqlite_admin.js)
// Todos los comportamientos fueron migrados a módulos ES en js/views/admin.js
// Este archivo permanece solo como stub temporal para evitar errores de caché del navegador.
// Puede eliminarse definitivamente tras un ciclo de despliegue.
(function(){
  if (window.console) console.info('sqlite_admin.js (legacy) ha sido deprecado. Usar js/views/admin.js');
})();
